ruff
mypy
django
sphinx
pytest
tox

uv init
uv add ruff
uv add django

# Create django project with name my_project_config in current directory
uv run django-admin startproject django_project .

uv run python manage.py migrate
uv run python manage.py runserver

uv run python manage.py startapp blog

# Creates the migrations to add any changes you've made to the models.py of your app (blog in this case)
uv run python manage.py makemigrations blog

# Prints the sql django prepares into the screen (in this case for app blog and migration 0001)
uv run python manage.py sqlmigrate blog 0001

# Checks for any problems in your project without making migrations or touching the database.
uv run python manage.py check

# Applies migrations
uv run python manage.py migrate

# django shell
uv run python manage.py shell

# Create admin user
uv run python manage.py createsuperuser

